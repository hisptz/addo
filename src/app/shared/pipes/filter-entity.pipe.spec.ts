import { FilterEntityPipe } from './filter-entity.pipe';

describe('FilterEntityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEntityPipe();
    expect(pipe).toBeTruthy();
  });
});
